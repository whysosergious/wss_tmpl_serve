export {
/**  all pkgs in readme  **/

    cq     as          Annotation,
    cr     as          AnnotationType,
    cS     as          BidiSpan,
    cT     as          BlockInfo,
    cU     as          BlockType,
    eW     as          Cassandra,
    bV     as          Change,
    cs     as          ChangeDesc,
    ct     as          ChangeSet,
    cu     as          CharCategory,
    bW     as          Chunk,
    fv     as          CodeMirror,
    fo     as          ColorType,
    cv     as          Compartment,
    u      as          CompletionContext,
    C      as          ContextTracker,
    cV     as          Decoration,
    cW     as          Direction,
    dL     as          DocInput,
    h      as          EditorSelection,
    cw     as          EditorState,
    g      as          EditorView,
    E      as          ExternalTokenizer,
    cx     as          Facet,
    dy     as          GenError,
    cX     as          GutterMarker,
    dM     as          HighlightStyle,
    dN     as          IndentContext,
    dD     as          InputStream,
    L      as          LRLanguage,
    e      as          LRParser,
    dO     as          Language,
    dP     as          LanguageDescription,
      b    as          LanguageSupport,
    cy     as          Line,
    dE     as          LocalTokenGroup,
    eX     as          MSSQL,
    cz     as          MapMode,
    eY     as          MariaSQL,
    cY     as          MatchDecorator,
    bX     as          MergeView,
    eZ     as          MySQL,
    e_     as          PLSQL,
    dQ     as          ParseContext,
    e$     as          PostgreSQL,
    cA     as          Prec,
    cB     as          Range,
    cC     as          RangeSet,
    cD     as          RangeSetBuilder,
    cE     as          RangeValue,
    cZ     as          RectangleMarker,
    c7     as          RegExpCursor,
    f0     as          SQLDialect,
    f1     as          SQLite,
    c8     as          SearchCursor,
    c9     as          SearchQuery,
    cF     as          SelectionRange,
    dF     as          Stack,
    f2     as          StandardSQL,
    cG     as          StateEffect,
    cH     as          StateEffectType,
    cI     as          StateField,
    dR     as          StreamLanguage,
    dS     as          StringStream,
    dG     as          StyleModule,
    cJ     as          Text,
    cK     as          Transaction,
    dT     as          TreeIndentContext,
    c_     as          ViewPlugin,
    c$     as          ViewUpdate,
    fw     as          Vim,
    d0     as          WidgetType,
    d1     as          __test,
    bY     as          acceptChunk,
    v      as          acceptCompletion,
    et     as          autoCloseTags,
    w      as          autocompletion,
    dH     as          base,
    dB     as          basicSetup,
    dU     as          bidiIsolates,
    a5     as          blockComment,
    a6     as          blockUncomment,
    dV     as          bracketMatching,
    dW     as          bracketMatchingHandle,
    dz     as          buildParser,
    dA     as          buildParserFile,
    x      as          clearSnippet,
    y      as          closeBrackets,
    z      as          closeBracketsKeymap,
    A      as          closeCompletion,
    d2     as          closeHoverTooltips,
    bJ     as          closeLintPanel,
    ca     as          closeSearchPanel,
    dX     as          codeFolding,
    cL     as          codePointAt,
    cM     as          codePointSize,
      $    as          collab,
    fj     as          color,
    fp     as          colorPicker,
    cN     as          combineConfig,
    eO     as          commonmarkLanguage,
    B      as          completeAnyWord,
    D      as          completeFromList,
    F      as          completionKeymap,
    eu     as          completionPath,
      G    as           completionStatus,
    fC     as          content,
      c    as           continuedIndent,
    a7     as          copyLineDown,
    a8     as          copyLineUp,
    cO     as          countColumn,
    fa     as          cpp,
    fb     as          cppLanguage,
    fn     as          createTheme,
    d3     as          crosshairCursor,
    eG     as          css,
    eH     as          cssCompletionSource,
    eI     as          cssLanguage,
      H    as           currentCompletions,
    a9     as          cursorCharBackward,
    aa     as          cursorCharForward,
    ab     as          cursorCharLeft,
    ac     as          cursorCharRight,
    ad     as          cursorDocEnd,
    ae     as          cursorDocStart,
    af     as          cursorGroupBackward,
    ag     as          cursorGroupForward,
    ah     as          cursorGroupLeft,
    ai     as          cursorGroupRight,
    aj     as          cursorLineBoundaryBackward,
    ak     as          cursorLineBoundaryForward,
    al     as          cursorLineBoundaryLeft,
    am     as          cursorLineBoundaryRight,
    an     as          cursorLineDown,
    ao     as          cursorLineEnd,
    ap     as          cursorLineStart,
    aq     as          cursorLineUp,
    ar     as          cursorMatchingBracket,
    as     as          cursorPageDown,
    at     as          cursorPageUp,
    au     as          cursorSubwordBackward,
    av     as          cursorSubwordForward,
    aw     as          cursorSyntaxLeft,
    ax     as          cursorSyntaxRight,
    dY     as          defaultHighlightStyle,
    ay     as          defaultKeymap,
      d    as           defineCSSCompletionSource,
    dZ     as          defineLanguageFacet,
      I    as           deleteBracketPair,
    az     as          deleteCharBackward,
    aA     as          deleteCharBackwardStrict,
    aB     as          deleteCharForward,
    aC     as          deleteGroupBackward,
    aD     as          deleteGroupForward,
    aE     as          deleteLine,
    aF     as          deleteLineBoundaryBackward,
    aG     as          deleteLineBoundaryForward,
    eP     as          deleteMarkupBackward,
    aH     as          deleteToLineEnd,
    aI     as          deleteToLineStart,
    aJ     as          deleteTrailingWhitespace,
      k    as           delimitedIndent,
    bK     as          diagnosticCount,
    bZ     as          diff,
    fA     as          dom,
    d4     as          drawSelection,
    d5     as          dropCursor,
    fz     as          element,
    aK     as          emacsStyleKeymap,
      d_     as          ensureSyntaxTree,
    ev     as          esLint,
    cP     as          findClusterBreak,
    cQ     as          findColumn,
    cb     as          findNext,
    cc     as          findPrevious,
    d$     as          flatIndent,
    e0     as          foldAll,
    e1     as          foldCode,
    e2     as          foldEffect,
    e3     as          foldGutter,
      a    as           foldInside,
    e4     as          foldKeymap,
      f    as           foldNodeProp,
    e5     as          foldService,
    e6     as          foldState,
    e7     as          foldable,
    e8     as          foldedRanges,
    bL     as          forEachDiagnostic,
    bM     as          forceLinting,
    e9     as          forceParsing,
    cR     as          fromCodePoint,
    fx     as          getCM,
      b_     as          getChunks,
    a0     as          getClientID,
    d6     as          getDrawSelectionConfig,
    ea     as          getIndentUnit,
    eb     as          getIndentation,
    b$     as          getOriginalDoc,
    d7     as          getPanel,
    cd     as          getSearchQuery,
    a1     as          getSyncedVersion,
    d8     as          getTooltip,
      q    as           go,
    c0     as          goToNextChunk,
    c1     as          goToPreviousChunk,
    ce     as          gotoLine,
    d9     as          gutter,
    da     as          gutterLineClass,
    db     as          gutters,
    dc     as          hasHoverTooltips,
      J    as           hasNextSnippetField,
      K    as           hasPrevSnippetField,
    dd     as          highlightActiveLine,
    de     as          highlightActiveLineGutter,
    cf     as          highlightSelectionMatches,
    df     as          highlightSpecialChars,
    dg     as          highlightTrailingWhitespace,
    dh     as          highlightWhitespace,
    ec     as          highlightingFor,
    aL     as          history,
    aM     as          historyField,
    aN     as          historyKeymap,
    di     as          hoverTooltip,
      n    as           html,
    fF     as          hyperLink,
    fD     as          hyperLinkExtension,
    fE     as          hyperLinkStyle,
      M    as           ifIn,
      N    as           ifNotIn,
    aO     as          indentLess,
    aP     as          indentMore,
      i    as           indentNodeProp,
    ed     as          indentOnInput,
    ee     as          indentRange,
    aQ     as          indentSelection,
    ef     as          indentService,
    eg     as          indentString,
    eh     as          indentUnit,
    aR     as          indentWithTab,
    aS     as          insertBlankLine,
      O    as           insertBracket,
      P    as           insertCompletionText,
    aT     as          insertNewline,
    aU     as          insertNewlineAndIndent,
    eQ     as          insertNewlineContinueMarkup,
    aV     as          insertNewlineKeepIndent,
    aW     as          insertTab,
    aX     as          invertedEffects,
    aY     as          isolateHistory,
    f8     as          java,
    f9     as          javaLanguage,
    ew     as          javascript,
      m    as           javascriptLanguage,
    eJ     as          json,
    eK     as          jsonLanguage,
    eL     as          jsonParseLinter,
    ex     as          jsxLanguage,
    dJ     as          keyName,
    dj     as          keymap,
    f3     as          keywordCompletion,
    f4     as          keywordCompletionSource,
    ei     as          language,
    ej     as          languageDataProp,
    dK     as          languages,
    dk     as          layer,
    eM     as          lezer,
    eN     as          lezerLanguage,
    aZ     as          lineComment,
    dl     as          lineNumberMarkers,
    dm     as          lineNumbers,
    fG     as          lineNumbersRelative,
      a_     as          lineUncomment,
    bN     as          lintGutter,
    bO     as          lintKeymap,
    bP     as          linter,
    ey     as          localCompletionSource,
    dn     as          logException,
    fq     as          makeColorPicker,
    eR     as          markdown,
    eS     as          markdownKeymap,
    eT     as          markdownLanguage,
    ek     as          matchBrackets,
    dC     as          minimalSetup,
      Q    as           moveCompletionSelection,
    a$     as          moveLineDown,
    b0     as          moveLineUp,
    bQ     as          nextDiagnostic,
      R    as           nextSnippetField,
    fk     as          oneDark,
    fl     as          oneDarkHighlightStyle,
    fm     as          oneDarkTheme,
    bR     as          openLintPanel,
    cg     as          openSearchPanel,
    c2     as          originalDocChangeEffect,
    dp     as          panels,
    fr     as          parseCallExpression,
    fs     as          parseColorLiteral,
    ft     as          parseNamedColor,
    eU     as          php,
    eV     as          phpLanguage,
      S    as           pickedCompletion,
    dq     as          placeholder,
    c3     as          presentableDiff,
      T    as           prevSnippetField,
    bS     as          previousDiagnostic,
      r    as           py,
    a2     as          rebaseUpdates,
    a3     as          receiveUpdates,
    dr     as          rectangularSelection,
    b1     as          redo,
    b2     as          redoDepth,
    b3     as          redoSelection,
    c4     as          rejectChunk,
    ch     as          replaceAll,
    ci     as          replaceNext,
    ds     as          repositionTooltips,
    dt     as          runScopeHandlers,
    eE     as          rust,
    eF     as          rustLanguage,
    f5     as          schemaCompletion,
    f6     as          schemaCompletionSource,
    ez     as          scopeCompletionSource,
    fB     as          scroll,
    du     as          scrollPastEnd,
    cj     as          search,
    ck     as          searchKeymap,
    cl     as          searchPanelOpen,
    b4     as          selectAll,
    b5     as          selectCharBackward,
    b6     as          selectCharForward,
    b7     as          selectCharLeft,
    b8     as          selectCharRight,
    b9     as          selectDocEnd,
    ba     as          selectDocStart,
    bb     as          selectGroupBackward,
    bc     as          selectGroupForward,
    bd     as          selectGroupLeft,
    be     as          selectGroupRight,
    bf     as          selectLine,
    bg     as          selectLineBoundaryBackward,
    bh     as          selectLineBoundaryForward,
    bi     as          selectLineBoundaryLeft,
    bj     as          selectLineBoundaryRight,
    bk     as          selectLineDown,
    bl     as          selectLineEnd,
    bm     as          selectLineStart,
    bn     as          selectLineUp,
    cm     as          selectMatches,
    bo     as          selectMatchingBracket,
    cn     as          selectNextOccurrence,
    bp     as          selectPageDown,
    bq     as          selectPageUp,
    br     as          selectParentSyntax,
    co     as          selectSelectionMatches,
    bs     as          selectSubwordBackward,
    bt     as          selectSubwordForward,
    bu     as          selectSyntaxLeft,
    bv     as          selectSyntaxRight,
      U    as           selectedCompletion,
      V    as           selectedCompletionIndex,
    a4     as          sendableUpdates,
    bT     as          setDiagnostics,
    bU     as          setDiagnosticsEffect,
    cp     as          setSearchQuery,
      W    as           setSelectedCompletion,
    dI     as          shift,
    dv     as          showPanel,
    dw     as          showTooltip,
    bw     as          simplifySelection,
      X    as           snippet,
      Y    as           snippetCompletion,
      Z    as           snippetKeymap,
    eA     as          snippets,
    bx     as          splitLine,
    f7     as          sql,
    by     as          standardKeymap,
      _    as           startCompletion,
    el     as          sublanguageProp,
    em     as          syntaxHighlighting,
    en     as          syntaxParserRunning,
      j    as           syntaxTree,
    eo     as          syntaxTreeAvailable,
    bz     as          temporarilySetTabFocusMode,
    bA     as          toggleBlockComment,
    bB     as          toggleBlockCommentByLine,
    bC     as          toggleComment,
    ep     as          toggleFold,
    bD     as          toggleLineComment,
    bE     as          toggleTabFocusMode,
    dx     as          tooltips,
    bF     as          transposeChars,
    eB     as          tsxLanguage,
    eC     as          typescriptLanguage,
    eD     as          typescriptSnippets,
    bG     as          undo,
    bH     as          undoDepth,
    bI     as          undoSelection,
    eq     as          unfoldAll,
    er     as          unfoldCode,
    es     as          unfoldEffect,
    c5     as          unifiedMergeView,
    c6     as          updateOriginalDoc,
    fy     as          vim,
    fc     as          vue,
    fd     as          vueLanguage,
    fe     as          wast,
    ff     as          wastLanguage,
    fu     as          wrapperClassName,
      o    as           xml,
    fg     as          yaml,
    fh     as          yamlFrontmatter,
    fi     as           yamlLanguage

/**  @these should work as if compiled with esm  **/
} from './chunks/mod.pme.js'
